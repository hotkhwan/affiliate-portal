# affiliate-portal

Public repository for the affiliate portal web service.

Application scaffolding is developed through feature branches and reviewed pull requests into `develop`. Production promotion flows from `develop` to `main` after validation.

